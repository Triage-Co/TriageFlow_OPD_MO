Hãy sửa `src/features/body-map/bodyRegions.ts` để các vùng body map có `searchPhrase` tiếng Anh đúng cho API Infermedica search.

Hiện tại API test thành công với:
`GET /api/infermedica/search?age=30&phrase=hand`

Vì vậy không dùng phrase kiểu `"hand pain"` nữa. Ưu tiên dùng từ vùng cơ thể đơn giản như `hand`, `leg`, `head`.

Yêu cầu:

1. Với mỗi `BodyRegion`, đảm bảo có:

* `id`
* `labelVi`
* `labelEn`
* `side`
* `searchPhrase`

2. Nếu type `BodyRegion` chưa có fallback, hãy thêm optional field:

```ts
fallbackSearchPhrases?: string[];
```

3. Cập nhật mapping theo bảng dưới đây:

```ts
const REGION_SEARCH_MAPPING: Record<string, {
  labelEn: string;
  searchPhrase: string;
  fallbackSearchPhrases?: string[];
}> = {
  // Head / face / neck
  "Đầu": {
    labelEn: "Head",
    searchPhrase: "head",
  },
  "Da đầu": {
    labelEn: "Scalp",
    searchPhrase: "scalp",
    fallbackSearchPhrases: ["head"],
  },
  "Mặt": {
    labelEn: "Face",
    searchPhrase: "face",
    fallbackSearchPhrases: ["head"],
  },
  "Trán": {
    labelEn: "Forehead",
    searchPhrase: "forehead",
    fallbackSearchPhrases: ["head"],
  },
  "Mắt": {
    labelEn: "Eye",
    searchPhrase: "eye",
  },
  "Tai": {
    labelEn: "Ear",
    searchPhrase: "ear",
  },
  "Mũi": {
    labelEn: "Nose",
    searchPhrase: "nose",
  },
  "Miệng": {
    labelEn: "Mouth",
    searchPhrase: "mouth",
  },
  "Môi": {
    labelEn: "Lip",
    searchPhrase: "lip",
    fallbackSearchPhrases: ["mouth"],
  },
  "Lưỡi": {
    labelEn: "Tongue",
    searchPhrase: "tongue",
    fallbackSearchPhrases: ["mouth"],
  },
  "Răng": {
    labelEn: "Tooth",
    searchPhrase: "tooth",
    fallbackSearchPhrases: ["mouth"],
  },
  "Nướu": {
    labelEn: "Gum",
    searchPhrase: "gum",
    fallbackSearchPhrases: ["mouth"],
  },
  "Hàm": {
    labelEn: "Jaw",
    searchPhrase: "jaw",
    fallbackSearchPhrases: ["face"],
  },
  "Cằm": {
    labelEn: "Chin",
    searchPhrase: "chin",
    fallbackSearchPhrases: ["face"],
  },
  "Cổ": {
    labelEn: "Neck",
    searchPhrase: "neck",
  },
  "Cổ họng": {
    labelEn: "Throat",
    searchPhrase: "throat",
    fallbackSearchPhrases: ["neck"],
  },

  // Torso front
  "Vai": {
    labelEn: "Shoulder",
    searchPhrase: "shoulder",
    fallbackSearchPhrases: ["arm"],
  },
  "Nách": {
    labelEn: "Armpit",
    searchPhrase: "armpit",
    fallbackSearchPhrases: ["shoulder", "arm"],
  },
  "Ngực": {
    labelEn: "Chest",
    searchPhrase: "chest",
  },
  "Vú": {
    labelEn: "Breast",
    searchPhrase: "breast",
    fallbackSearchPhrases: ["chest"],
  },
  "Bụng": {
    labelEn: "Abdomen",
    searchPhrase: "abdomen",
  },
  "Bụng trên": {
    labelEn: "Upper abdomen",
    searchPhrase: "upper abdomen",
    fallbackSearchPhrases: ["abdomen"],
  },
  "Bụng dưới": {
    labelEn: "Lower abdomen",
    searchPhrase: "lower abdomen",
    fallbackSearchPhrases: ["abdomen"],
  },
  "Rốn": {
    labelEn: "Navel",
    searchPhrase: "navel",
    fallbackSearchPhrases: ["abdomen"],
  },
  "Hông": {
    labelEn: "Hip",
    searchPhrase: "hip",
    fallbackSearchPhrases: ["abdomen", "leg"],
  },
  "Bẹn": {
    labelEn: "Groin",
    searchPhrase: "groin",
    fallbackSearchPhrases: ["abdomen", "hip"],
  },
  "Vùng sinh dục": {
    labelEn: "Genitals",
    searchPhrase: "genitals",
    fallbackSearchPhrases: ["groin"],
  },

  // Back
  "Lưng": {
    labelEn: "Back",
    searchPhrase: "back",
  },
  "Lưng trên": {
    labelEn: "Upper back",
    searchPhrase: "upper back",
    fallbackSearchPhrases: ["back"],
  },
  "Lưng dưới": {
    labelEn: "Lower back",
    searchPhrase: "lower back",
    fallbackSearchPhrases: ["back"],
  },
  "Cột sống": {
    labelEn: "Spine",
    searchPhrase: "spine",
    fallbackSearchPhrases: ["back"],
  },
  "Eo": {
    labelEn: "Waist",
    searchPhrase: "waist",
    fallbackSearchPhrases: ["lower back", "abdomen"],
  },
  "Mông": {
    labelEn: "Buttock",
    searchPhrase: "buttock",
    fallbackSearchPhrases: ["hip", "back"],
  },

  // Upper limb
  "Tay": {
    labelEn: "Arm",
    searchPhrase: "arm",
    fallbackSearchPhrases: ["hand"],
  },
  "Cánh tay": {
    labelEn: "Arm",
    searchPhrase: "arm",
  },
  "Bắp tay": {
    labelEn: "Upper arm",
    searchPhrase: "upper arm",
    fallbackSearchPhrases: ["arm"],
  },
  "Khuỷu tay": {
    labelEn: "Elbow",
    searchPhrase: "elbow",
    fallbackSearchPhrases: ["arm"],
  },
  "Cẳng tay": {
    labelEn: "Forearm",
    searchPhrase: "forearm",
    fallbackSearchPhrases: ["arm"],
  },
  "Cổ tay": {
    labelEn: "Wrist",
    searchPhrase: "wrist",
    fallbackSearchPhrases: ["hand", "arm"],
  },
  "Bàn tay": {
    labelEn: "Hand",
    searchPhrase: "hand",
  },
  "Lòng bàn tay": {
    labelEn: "Palm",
    searchPhrase: "palm",
    fallbackSearchPhrases: ["hand"],
  },
  "Mu bàn tay": {
    labelEn: "Back of hand",
    searchPhrase: "hand",
  },
  "Ngón tay": {
    labelEn: "Finger",
    searchPhrase: "finger",
    fallbackSearchPhrases: ["hand"],
  },
  "Ngón cái": {
    labelEn: "Thumb",
    searchPhrase: "thumb",
    fallbackSearchPhrases: ["finger", "hand"],
  },

  // Lower limb
  "Chân": {
    labelEn: "Leg",
    searchPhrase: "leg",
  },
  "Đùi": {
    labelEn: "Thigh",
    searchPhrase: "thigh",
    fallbackSearchPhrases: ["leg"],
  },
  "Đầu gối": {
    labelEn: "Knee",
    searchPhrase: "knee",
    fallbackSearchPhrases: ["leg"],
  },
  "Cẳng chân": {
    labelEn: "Lower leg",
    searchPhrase: "lower leg",
    fallbackSearchPhrases: ["leg"],
  },
  "Ống chân": {
    labelEn: "Shin",
    searchPhrase: "shin",
    fallbackSearchPhrases: ["lower leg", "leg"],
  },
  "Bắp chân": {
    labelEn: "Calf",
    searchPhrase: "calf",
    fallbackSearchPhrases: ["lower leg", "leg"],
  },
  "Cổ chân": {
    labelEn: "Ankle",
    searchPhrase: "ankle",
    fallbackSearchPhrases: ["foot", "leg"],
  },
  "Bàn chân": {
    labelEn: "Foot",
    searchPhrase: "foot",
  },
  "Lòng bàn chân": {
    labelEn: "Sole",
    searchPhrase: "sole",
    fallbackSearchPhrases: ["foot"],
  },
  "Gót chân": {
    labelEn: "Heel",
    searchPhrase: "heel",
    fallbackSearchPhrases: ["foot"],
  },
  "Ngón chân": {
    labelEn: "Toe",
    searchPhrase: "toe",
    fallbackSearchPhrases: ["foot"],
  },
};
```

4. Cập nhật logic search trong `src/features/triage/context/TriageContext.tsx` hoặc service liên quan:

Nếu gọi API bằng `searchPhrase` mà trả mảng rỗng, tự thử lần lượt `fallbackSearchPhrases`.

Ví dụ:

```ts
const phrasesToTry = [
  searchPhrase,
  ...(fallbackSearchPhrases ?? []),
];

for (const phrase of phrasesToTry) {
  const result = await triageApiService.searchSymptoms({
    age,
    phrase,
  });

  if (Array.isArray(result) && result.length > 0) {
    // dùng result này
    break;
  }
}
```

5. Khi truyền từ `body-map.tsx` sang `symptoms.tsx`, truyền thêm:

* `searchPhrase`
* `fallbackSearchPhrases` dạng JSON string nếu cần

Ví dụ:

```ts
router.push({
  pathname: "/(patient)/visit/symptoms",
  params: {
    regionId: selectedRegion.id,
    labelVi: selectedRegion.labelVi,
    labelEn: selectedRegion.labelEn,
    searchPhrase: selectedRegion.searchPhrase,
    fallbackSearchPhrases: JSON.stringify(selectedRegion.fallbackSearchPhrases ?? []),
  },
});
```

6. Trong `symptoms.tsx`, parse lại:

```ts
const fallbackSearchPhrases = params.fallbackSearchPhrases
  ? JSON.parse(String(params.fallbackSearchPhrases))
  : [];
```

7. Log debug rõ:

```ts
console.log("[Triage] Search phrases to try:", phrasesToTry);
```

8. Cache symptom search:

* Key vẫn theo `age + phrase`
* Nếu phrase đầu không có data thì không cache mảng rỗng
* Chỉ cache phrase nào trả về data

9. Test bắt buộc:

* Bàn tay phải gọi được phrase `"hand"`
* Chân phải gọi được phrase `"leg"`
* Đầu phải gọi được phrase `"head"`
* Bàn chân phải gọi được phrase `"foot"`
* Ngực phải gọi được phrase `"chest"`
* Bụng phải gọi được phrase `"abdomen"`

10. TranslationService chỉ dùng để dịch response EN → VI cho UI, không dùng để đoán search phrase trong body map. Search phrase phải đến từ `bodyRegions.ts`.
